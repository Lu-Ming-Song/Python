convert()用于颜色转换

save()用于将图片保存成不同的格式

thumbnail()方法用于将图片转换成指定大小的缩略图，参数是元组

crop()方法用于从一幅图中裁剪指定区域，注意rgb图像定义坐标系原点在左上

transpose（）方法用于旋转图片，paste()方法用于将图片放到指定区域

resize()方法用于调整图像的尺寸，参数是元组，用于指定新图像的大小

rotate()方法用于逆时针旋转图像

